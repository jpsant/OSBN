import React from 'react';
import classes from './css/historyDiv.module.css';

const historyDiv = (props) => {
    return (
        <h1>hello from historyDiv!</h1>
    );
}

export default historyDiv;